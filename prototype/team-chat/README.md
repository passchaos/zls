# Team chat prototype

Open `index.html` in a browser, or serve this directory with any static file server. The prototype uses Lucide icons from a CDN, so keep a network connection available when previewing it.

Run the interaction helper tests with:

```sh
npm test
```
